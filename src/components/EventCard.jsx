import React from "react";
import { Link } from "react-router-dom";

const EventCard = ({ oneEvent, setEvents }) => {
  return (
    <div>
      <section>
        {/* <img> </img> */}
        <h4> </h4>
        <p> </p>
      </section>
    </div>
  );
};

export default EventCard;
